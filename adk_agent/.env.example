# ADK Agent Environment Configuration
# For use with Google Agent Development Kit

# REQUIRED: Google API Key for Gemini models
# ============================================================================
# ADK + LiteLLM + Ollama Configuration
# ============================================================================
# This agent uses LiteLLM to connect ADK to Ollama self-hosted models

# Ollama API Base URL (required for LiteLLM ollama_chat provider)
# Make sure Ollama is running: ollama serve
OLLAMA_API_BASE=http://localhost:11434

# Optional: Model selection (default: mistral)
# Models with tool support recommended: mistral-small3.1, llama3.2, etc.
# Check tool support: ollama show <model-name>
# OLLAMA_MODEL=mistral

# ============================================================================
# Notes:
# - No API key needed for Ollama (self-hosted)
# - LiteLLM uses ollama_chat provider for better tool calling behavior
# - Alternative: Use openai provider with OPENAI_API_BASE=http://localhost:11434/v1
# ============================================================================

# Model Parameters
TEMPERATURE=0.2
MAX_OUTPUT_TOKENS=2048
CONTEXT_WINDOW=32768

# Scraping Configuration
MAX_WORDS=25000
REQUEST_TIMEOUT=30
